name: synapsis_survey
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  top_modal_sheet: ^2.0.1
  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.2.1
  sqflite: ^2.3.0
  dartz: ^0.10.1
  equatable: ^2.0.5
  get_it: ^7.6.4
  intl: ^0.18.1
  page_view_indicators: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/